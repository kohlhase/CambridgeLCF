rotate	- reorders subgoals on top of stack

rotate: int -> void
(part of subgoal package)

Calling (rotate n) rotates, by n steps, the set of subgoals on top of the
stack.  Thus you can consider the subgoals of a goal in any order.  However,
you cannot consider subgoals deeper in the stack.

SEE ALSO

set_goal   (for description of subgoal package)
expandf
expand
backup
save_top_thm
top_goal
get_state
set_state
print_state
