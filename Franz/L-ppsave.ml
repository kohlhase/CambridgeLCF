(****************************************************************************
*
*	PPSAVE
*
*****************************************************************************
* Original: ppsave (Cambridge LCF)
* Converted to Standard ML by P M Hedlund, Rutherford Appleton Lab. (1986)
* All the theorems of PPLAMB. This file should be loaded only when
* doing PPLAMB theory.
*)

save_thm ("EQ_REFL", EQ_REFL);
save_thm ("EQ_ANAL", EQ_ANAL);
save_thm ("EQ_SYM", EQ_SYM);
save_thm ("EQ_TRANS", EQ_TRANS);
save_thm ("EQ_EXT", EQ_EXT);
save_thm ("TR_EQ_DISTINCT", TR_EQ_DISTINCT);
save_thm ("MIN_COMB", MIN_COMB);
save_thm ("MIN_ABS", MIN_ABS);
save_thm ("ETA_EQ", ETA_EQ);
save_thm ("LESS_UU", LESS_UU);
save_thm ("FST_STRICT", FST_STRICT);
save_thm ("SND_STRICT", SND_STRICT);
